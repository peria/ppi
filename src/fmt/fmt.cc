#include "fmt/fmt.h"

#include "base/base.h"

namespace ppi {
namespace fmt {

void Fmt::Fmt4(double* da, const int64 n, const Type type) {
  UNUSED_VARIABLE(da);
  UNUSED_VARIABLE(n);
  UNUSED_VARIABLE(type);
}

}  // namespace fmt
}  // namespace ppi
